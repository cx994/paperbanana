# PaperBanana Environment Variables
# Copy this file to .env and fill in your keys.

# OpenAI (alternative): https://platform.openai.com/api-keys
# To use OpenAI instead of Gemini, uncomment these:
# VLM_PROVIDER=openai
# IMAGE_PROVIDER=openai_imagen
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_VLM_MODEL=gpt-5.2
OPENAI_IMAGE_MODEL=gpt-image-1.5
# For Azure OpenAI / Foundry, point to your endpoint:
#   OPENAI_BASE_URL=https://<resource>.openai.azure.com/openai/v1

# Google Gemini (free, no credit card): https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=

# Optional: route google-genai SDK through a custom API gateway/proxy.
# If set, PaperBanana will create the client with:
#   vertexai=<true|false> and http_options={"base_url": ..., "headers": {"Authorization": ...}}
# GOOGLE_GENAI_BASE_URL=https://your-proxy.example.com
# Set to false if your gateway implements the Gemini Developer API style paths:
#   /v1beta/models/{model}:generateContent
# Note: when set to false, the google-genai SDK requires an API key value
# (GOOGLE_API_KEY or VLM_/IMAGE_ overrides), even if your gateway auth is via Authorization.
# GOOGLE_GENAI_USE_VERTEXAI=true
# Either provide the full Authorization header value (recommended)...
# GOOGLE_GENAI_AUTHORIZATION=Bearer your_token
# ...or just the token (PaperBanana will prefix with "Bearer "):
# GOOGLE_GENAI_AUTH_TOKEN=your_token

# Per-component overrides (optional). If set, these take precedence over the global GOOGLE_* values.
#
# ── VLM (Flash / planning / critique) ───────────────────────────────
# VLM_GOOGLE_API_KEY=
# VLM_GOOGLE_GENAI_BASE_URL=
# VLM_GOOGLE_GENAI_USE_VERTEXAI=
# VLM_GOOGLE_GENAI_AUTHORIZATION=Bearer your_token
# VLM_GOOGLE_GENAI_AUTH_TOKEN=your_token
#
# ── Image Generation (Imagen) ───────────────────────────────────────
# IMAGE_GOOGLE_API_KEY=
# IMAGE_GOOGLE_GENAI_BASE_URL=
# IMAGE_GOOGLE_GENAI_USE_VERTEXAI=
# IMAGE_GOOGLE_GENAI_AUTHORIZATION=Bearer your_token
# IMAGE_GOOGLE_GENAI_AUTH_TOKEN=your_token

# ── SSL ────────────────────────────────────────────────────────────
# Set to true to skip SSL certificate verification (corporate proxies)
SKIP_SSL_VERIFICATION=false
